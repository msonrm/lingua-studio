# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code のローカル設定（各自の環境依存）
.claude/settings.local.json

# 実験の生出力（結果は docs/EXPERIMENT-RESULT.md に要約）

# Phase 0 の生成物。決定的かつ LLM 不要で数秒で再生成できるので追跡しない
# （LLM の応答は再生成できないので experiments/out/phase*.json は追跡する）
experiments/out/all.json
experiments/out/collisions-en.json
experiments/out/collisions-ja.json
